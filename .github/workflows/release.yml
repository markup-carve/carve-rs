name: Release

# Publishing a GitHub Release (draft -> publish) creates the tag and holds the
# notes; this workflow then publishes the crate to crates.io. The published
# Release is the single source of truth - there is no in-repo release-notes file
# and no separate job that creates the Release.
on:
  release:
    types: [published]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

jobs:
  publish:
    name: Publish to crates.io
    runs-on: ubuntu-latest
    permissions:
      id-token: write # OIDC for crates.io Trusted Publishing
      contents: read
    env:
      # First publish of a brand-new crate cannot use OIDC Trusted Publishing
      # (no crate yet = no trusted-publisher config to point at). Set a
      # CARGO_REGISTRY_TOKEN secret to bootstrap the first release; once the
      # crate exists, configure Trusted Publishing on crates.io and delete the
      # secret to fall back to tokenless OIDC.
      HAS_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN != '' }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name }}
          submodules: recursive

      # Guard: the crate version must match the release tag, so a mistyped tag
      # or a forgotten version bump cannot publish the wrong version.
      - name: Verify Cargo.toml version matches the release tag
        run: |
          TAG="${{ github.event.release.tag_name }}"
          VER="$(grep -m1 -E '^version[[:space:]]*=' Cargo.toml | sed -E 's/.*"([^"]+)".*/\1/')"
          if [ "$TAG" != "$VER" ]; then
            echo "::error::Release tag $TAG does not match Cargo.toml version $VER."
            exit 1
          fi

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      # Skip OIDC when a bootstrap token is present; otherwise mint a
      # short-lived token via Trusted Publishing (configured on crates.io).
      - uses: rust-lang/crates-io-auth-action@v1
        id: auth
        if: env.HAS_TOKEN != 'true'

      - run: cargo publish
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN || steps.auth.outputs.token }}
